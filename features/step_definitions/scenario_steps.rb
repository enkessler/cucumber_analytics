Then /^scenario "([^"]*)" is found to have the following properties:$/ do |arg, table|
  pending
end

Then /^scenario "([^"]*)" descriptive lines are as follows:$/ do |arg, table|
  pending
end

Then /^scenario "([^"]*)" steps are as follows:$/ do |arg, table|
  pending
end

Then /^scenario "([^"]*)" is found to have the following tags:$/ do |arg, table|
  pending
end
